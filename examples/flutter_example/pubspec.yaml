name: appbase_flutter_example
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.8.0
dependencies:
  appbase_client: ^0.1.0
  appbase_drift: ^0.1.0
  appbase_flutter: ^0.1.0
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
