# Flutter integration example

This package is intentionally a product-shell example rather than a second
backend. See `../../docs/flutter-integration.md` for the complete wiring,
migration, account-switch, and collection-adapter example.
