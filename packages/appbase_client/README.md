# appbase_client

Pure Dart AppBase HTTP client, typed failures, protocol models, and non-reentrant
pull/push synchronization engine. It has no Flutter, database, OIDC, or product
dependency. Start with the repository's `docs/flutter-integration.md` and
`protocol/semantics.md`.
