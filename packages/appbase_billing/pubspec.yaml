name: appbase_billing
description: Account-bound billing orchestration and HTTP client for AppBase.
version: 0.1.0
resolution: workspace
repository: https://github.com/saltbo/appbase
environment:
  sdk: ^3.12.0
  flutter: '>=3.41.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
dev_dependencies:
  flutter_test:
    sdk: flutter
