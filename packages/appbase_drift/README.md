# appbase_drift

Generic Drift account metadata, record revisions, and transaction-safe outbox
for `appbase_client`. Product collection adapters and AppBase metadata execute
inside the product database transaction.
