name: appbase_flutter
description: Flutter lifecycle, secure installation identity, and OIDC adapters for AppBase.
version: 0.1.0
resolution: workspace
repository: https://github.com/saltbo/appbase
issue_tracker: https://github.com/saltbo/appbase/issues
environment:
  sdk: ^3.8.0
  flutter: '>=3.32.0'
dependencies:
  appbase_client: ^0.1.0
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  oidc: ^0.14.0+2
  oidc_default_store: ^0.6.0+2
dev_dependencies:
  flutter_test:
    sdk: flutter
