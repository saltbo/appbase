import { D1AccountRepository } from "./d1_accounts.js";
/** Compatibility name; lifecycle state now belongs to appbase_accounts. */
export class D1AccountDeletionRepository extends D1AccountRepository {}
